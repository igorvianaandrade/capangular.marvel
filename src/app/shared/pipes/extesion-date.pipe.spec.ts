import { ExtesionDatePipe } from './extesion-date.pipe';

describe('ExtesionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtesionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
